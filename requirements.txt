fastapi
uvicorn[standard]
openai>=1.0.0
jinja2
pydantic
pyyaml
python-multipart
tenacity
python-dotenv
json-repair
Pillow

# LangGraph + Langfuse engine (Phase 1 pilot, deeppresenter/graph/) — ChatOpenAI, not
# ChatAnthropic: the app talks to its configured model through an OpenAI-compatible
# base_url (OPENAI_BASE_URL) even when that model is Claude.
langchain
langchain-core
langchain-openai
langgraph
langfuse
